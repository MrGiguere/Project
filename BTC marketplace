#Create different wallets for different users
#Create a mine function that gives a set amount of BTC
#Transfer bitcoin between wallets
#Destroy wallet

userDict = {}

class Wallet:

def __init__(self,username,password)

def createWallet(username, password):            #create a new user
    userDict[user] = amount


def deleteWallet(address, password):                    #delete a user
    userDict.pop(user)

    



























